export enum EExecutionAccountsMethods {
  UPDATE_STATUS = "UPDATE_STATUS",
  RESTART_ACTION = "RESTART_ACTION",
  SKIP_CURRENT_STEP = "SKIP_CURRENT_STEP",
}

export enum EExecutionStepStateMethods {
  UPDATE_STATUS = "UPDATE_STATUS",
}
